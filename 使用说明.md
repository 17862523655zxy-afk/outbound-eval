# 外呼 Agent 评测平台 - 使用说明

## 快速启动

### macOS / Linux
双击运行 `start.sh` 或在终端执行：
```bash
./start.sh
```

### Windows
双击运行 `start.bat`

---

## 访问地址

启动后在浏览器打开：**http://localhost:8000**

---

## 配置 API Key

首次使用需要配置 LLM API Key：

1. 编辑项目根目录下的 `.env` 文件
2. 填入你的 API Key：

```env
OPENAI_API_KEY=sk-xxxxx
BASE_URL=https://api.deepseek.com
LLM_MODEL=deepseek-chat
```

支持 OpenAI / DeepSeek / 其他兼容 API。

---

## 使用流程

### 方式一：Dashboard 网页操作

1. 打开 http://localhost:8000
2. 点击「评测任务」选择任务
3. 设置场景数量、难度等参数
4. 点击「开始评测」
5. 等待完成后查看报告

### 方式二：命令行操作

```bash
# 运行单个评测
python -m outbound_eval run_eval --task feimaotui_contract --scenarios 10

# 批量评测
python -m outbound_eval batch_eval --scenarios 10

# 从指令文件生成评测任务
python -m outbound_eval parse_instruction_cmd 任务说明.xlsx --task-id my_task

# 生成报告
python -m outbound_eval gen_report --task-id feimaotui_contract
```

---

## 评测报告位置

```
data/results/reports/
├── run_xxx.html   # HTML 报告（可直接浏览器打开）
├── run_xxx.md     # Markdown 报告
└── run_xxx.pdf    # PDF 报告（需安装 weasyprint）
```

---

## 评测维度

| 维度 | 说明 | 权重 |
|------|------|------|
| Task Success | 任务完成度 | 40% |
| Flow Adherence | 流程执行 | 20% |
| State Tracking | 状态记录 | 10% |
| Compliance | 话术合规 | 10% |
| Naturalness | 对话自然度 | 10% |
| Efficiency | 成本效率 | 10% |

---

## 常见问题

**Q: 启动报错 "Module not found"**
A: 运行 `pip install -r requirements.txt` 安装依赖

**Q: 评测卡住不动**
A: 检查 `.env` 中的 API Key 和 BASE_URL 是否正确

**Q: 如何添加新的评测任务**
A: 在 `data/benchmarks/tasks/` 目录下添加 YAML 配置文件

---

## 技术支持

如遇问题，请检查：
1. Python 版本 >= 3.10
2. 依赖是否完整安装
3. API Key 是否有效
